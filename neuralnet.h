#ifndef NEURALNET_H
#define NEURALNET_H

#include <vector>
#include "sane.h"

//TODO Encapsulate!

/*class neuron {
public:
    neuron();
    double value;
    std::vector<std::pair<neuron *, double>> weights;
};

class neuralnet {
public:
    neuralnet();
    std::vector<neuron> neurons;
    std::vector<neuron *> input_neurons;
    std::vector<neuron *> output_neurons;

    void from_genome(genome g);
    std::vector<double> evaluate(std::vector<double> inputs);
};*/

#endif // NEURALNET_H

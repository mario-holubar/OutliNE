#include "neuralnet.h"
#include <iostream>

/*neuron::neuron() {

}

neuralnet::neuralnet() {

}

void neuralnet::from_genome(genome g) {
    for (unsigned int i = 0; i < g.genes.size(); i++) {

    }
}

std::vector<double> neuralnet::evaluate(std::vector<double> inputs) {
    std::vector<double> outputs(output_neurons.size());
    if (inputs.size() != input_neurons.size()) {
        std::cerr << "Neural net input dimensions don't match";
        return outputs;
    }
    //
    return outputs;
}*/
